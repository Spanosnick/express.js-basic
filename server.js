const express = require('express');
const app = express();

// Set the routing
app.get('/', (req, res) => {
    // res.json({ message: 'Hello World!' });
    // res.send('Hello World!');
    // res.download('./server.js');
    // res.render
});


app.listen(3000);

